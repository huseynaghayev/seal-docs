{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to Seal Documentation","text":"<p>Seal is a scripting language that is beautiful, efficient and embeddable.</p>"},{"location":"#getting-started","title":"Getting Started","text":"<p>If you are new to Seal, check this.</p>"},{"location":"getting-started/","title":"Installing Seal","text":"<p>You can install Seal by either directly cloning from GitHub and building, or downloading pre-built binaries.</p>"},{"location":"getting-started/#building-from-source","title":"Building from Source","text":"<p>Paste this code to terminal:</p> <pre><code>git clone https://github.com/huseynaghayev/seal\ncd seal\n# configure compiler or flags in Makefile if needed\nmake\n./seal --version # should print version\n</code></pre>"},{"location":"syntax/","title":"Syntax","text":"<p>Seal has a beautiful syntax that is easy to read and write. This section demonstrates the syntax of Seal.</p>"},{"location":"syntax/#overview","title":"Overview","text":"<p>Seal uses indentation for block definitions unlike C-like languages which use <code>{ }</code> (Curly Braces). With this approach, code looks cleaner and more natural. Unlike Python, it does not use <code>:</code> (Colons) at the end which I dislike a lot. Example Seal program:</p> <pre><code>define is_prime(n)\n    if n &lt; 2 then return false\n\n    i = 2\n    while i * i &lt;= n\n        if n % i == 0\n            return false\n\n        i += 1\n\n    return true\n\n\nprint(is_prime(127))\n</code></pre>"},{"location":"syntax/#sections","title":"Sections","text":"<ul> <li> <p>Comments</p> </li> <li> <p>Variables</p> </li> <li> <p>Data Types</p> </li> <li> <p>Operators</p> </li> <li> <p>Control Flow</p> </li> <li> <p>Functions</p> </li> <li> <p>Strings</p> </li> <li> <p>Lists</p> </li> <li> <p>Maps (Hashmaps / Tables / Objects)</p> </li> </ul>"},{"location":"syntax/comment/","title":"Comments","text":"<p>In Seal, comments are like in C/C++. <code>/**/</code> and <code>//</code>.</p>"},{"location":"syntax/comment/#multi-line-comments","title":"Multi-Line Comments","text":"<p>Since the syntax is white-space sensitive (indentation-based), there are some restrictions for using <code>/**/</code> comments.</p> <p>You cannot use multi-line comments if a token comes right after comment for the first time. Example:</p> <pre><code>/* won't work */ if true\n\u00a0\u00a0\u00a0\u00a0print(\"something\")\n\nif /* will work */ true\n\u00a0\u00a0\u00a0\u00a0print(\"something else)\n</code></pre> <p><code>/**/</code> is not nested for now, but hopefully will be implemented in the future.</p> <pre><code>/* /* */ */ // fail\n</code></pre>"},{"location":"syntax/comment/#single-line-comments","title":"Single-Line Comments","text":"<p><code>//</code> comment can be used anywhere in the code</p> <pre><code>if true // this is a single-line comment\n\u00a0\u00a0\u00a0\u00a0print(\"hello\")\nelse\n// this is else branch\n\u00a0\u00a0\u00a0\u00a0print(\"goodbye\")\n</code></pre>"},{"location":"syntax/control/","title":"Control Flow","text":"<p>In Seal, control flow is straightforward. It has off-side rule aka indentation rule for defining where blocks start and end like Python, CoffeeScript, ABC.</p>"},{"location":"syntax/control/#if-else","title":"If-Else","text":"<pre><code>if a &gt; 0\n    print(\"a is positive\")\nelse if a &lt; 0\n    print(\"a is negative\")\nelse\n    print(\"a is zero\")\n</code></pre> <p>You just use indentation to define blocks. No <code>:</code> unlike Python.</p> <pre><code>a = integer(read())\n\nif a == 0 then print(\"zero\")\nelse if a == 1\n    print(\"it is one\")\nelse if a == 2 then print(\"two\")\nelse print(\"unknown\")\n</code></pre> <p>You can mix inline if-else statements with block ones.</p>"},{"location":"syntax/control/#while","title":"While","text":"<pre><code>while true\n    print(\"this will print forever\")\n</code></pre> <p>While loop is straightforward as well.</p> <pre><code>while true do print(\"hello\")\n</code></pre> <p>You can write inline while loop with <code>do</code> keyword.</p>"},{"location":"syntax/control/#seal-style-loop","title":"Seal-style loop","text":"<pre><code>i = -1\nmax = 10\nwhile ++i &lt; max\n    print(i) \n</code></pre> <p>Instead of putting <code>++i</code> at the end of the loop body, you can put the increment into the condition check itself, and compensate by assigning <code>-1</code> to <code>i</code> before the loop begins. So it becomes kind of a \"C-style for loop\". If you'll let me, I want to call this \"Seal-style loop\".</p>"},{"location":"syntax/control/#for","title":"For","text":"<pre><code>for i in 10\n    print(i) // from 0 to 10 (10 is excluded)\n\nfor c in \"my string\"\n    print(c)\n\nfor x in [1, 2, 3, 4, 5]\n    print(x)\n</code></pre> <p>Currently, for loop supports 3 types of iterables: integer, string and list.</p> <pre><code>sum = 0\nfor i in 10 do sum += i\nprint(sum)\n</code></pre> <p>Here is inline for loop as well.</p>"},{"location":"syntax/control/#skip-and-stop","title":"Skip and Stop","text":"<pre><code>for c in \"Hello, World!\"\n    if c == \"l\" then skip\n    if c == \"r\"\n        stop\n\n    print(c)\n</code></pre> <p>Output:</p> <pre><code>H\ne\no\n,\n\nW\no\n</code></pre> <p>As you can tell, <code>skip</code> is for the traditional <code>continue</code> keyword, and <code>stop</code> is for <code>break</code>. I have used these keywords since the first version of Seal, and decided to keep them. Because why not?</p>"},{"location":"syntax/control/#function","title":"Function","text":"<pre><code>define is_odd(n)\n    return n % 2 == 1\n</code></pre> <p>This code defines a local function called \"is_odd\" in current scope.</p> <pre><code>is_odd = define(n)\n    return n % 2 == 1\n</code></pre> <p>Since functions are first-class citizens in Seal, you can create an anonymous function and assign that directly to a variable. For convention, first option is more recommended.</p> <pre><code>define optimized_is_odd(n) (n &amp; 1) == 1\n</code></pre> <p>If closing parenthesis is followed by a single expression right away, it is called \"lambda\".</p> <pre><code>add = define(a, b) a + b\n</code></pre> <p>Again, first-class.</p> <pre><code>define $add(a, b) a + b\n$add = define(a, b) a + b\n\ndefine $add(a, b)\n    return a + b\n\n$add = define(a, b)\n    return a + b\n</code></pre> <p>By putting <code>$</code>, you define that function as global.</p>"},{"location":"syntax/control/#include","title":"Include","text":"<pre><code>include \"io\"\n</code></pre> <p>Include statement requires string after it. Here are standard library names:</p> <ul> <li> <p>\"math\"</p> </li> <li> <p>\"io\"</p> </li> <li> <p>\"system\"</p> </li> </ul> <p>When you include a file, you get capital case named global variable as a convention.</p> <pre><code>include \"math\"\nMath // global hashmap holding mathematical functions\ninclude \"io\"\nIO  // again, global hashmap holding IO-related functions\ninclude \"io\"  //  does not load again\n</code></pre> <p>When include statement is executed, first, it looks for standard libraries. Then, local files. It can include both <code>so</code>, <code>dll</code> files or Seal scripts. It starts with shared object files, and then Seal files. It loads the file only once.</p>"},{"location":"syntax/operators/","title":"Operators","text":""},{"location":"syntax/operators/#precedence-table","title":"Precedence Table","text":"<p>Seal follows almost exactly C's precende order for operators. Here is precedence table of Seal operators in descending order:</p> Operator(s) Description Associativity <code>++</code>, <code>--</code><code>()</code><code>[]</code> <code>.</code><code>-&gt;</code><code>?</code><code>$</code> Postfix increment and decrementFunction callIndexingField accessMethod callSafe accessGlobal variable access Left-to-right <code>++</code>,\u00a0<code>--</code><code>+</code>, <code>-</code> <code>!</code>,<code>not</code>, <code>~</code> Prefix increment and decrementUnary plus and minusLogical NOTs and bitwise NOT Right-to-left <code>*</code>, <code>/</code>, <code>%</code> Multiplication, division, and remainder Left-to-right <code>+</code>, <code>-</code> Addition and subtraction <code>&lt;&lt;</code>, <code>&gt;&gt;</code> Bitwise left shift and right shift <code>&lt;</code>,\u00a0<code>&lt;=</code><code>&gt;</code>,\u00a0<code>&gt;=</code> Less than and less than or equal toGreater than and greater than or equal to <code>==</code>, <code>!=</code> Equal to and not equal to <code>&amp;</code> Bitwise AND <code>^</code> Bitwise XOR (exclusive OR) <code>\\|</code> Bitwise OR <code>and</code> Logical AND <code>or</code> Logical OR <code>if then else</code> Ternary Right-to-left <code>=</code><code>*=</code>, <code>/=</code>, <code>%=</code><code>+=</code>, <code>-=</code><code>&lt;&lt;=</code>, <code>&gt;&gt;=</code>,<code>&amp;=</code>, <code>^=</code>, <code>\\|=</code> Simple assignmentAssignment by product, quotient, and remainderAssignment by sum and differenceAssignment by bitwise left shift and right shiftAssignment by bitwise AND, XOR, and OR <code>,</code> Comma Left-to-right"},{"location":"syntax/operators/#some-notes","title":"Some notes","text":"<ul> <li><code>++</code>, <code>--</code> are just like in C. If it is postfix, it mutates variable afterwards, otherwise, it mutates right before giving value.</li> </ul> <pre><code>a = 32\nprint(a++) //  32\nprint(a)   //  33\nprint(++a) //  34\nprint(a)   //  34\n</code></pre> <ul> <li><code>$</code> lets you access to a global symbol explicitly. It is must be used if mutation or declaration of a global symbol is desired.</li> </ul> <pre><code>$a = 20   //  global\na  = 30   //  local, since on assignment, globals must be explicit\nprint(a)  //  30, if no '$', locals are searched first\nprint($a) //  20, since it is explicitly referred to a global\n</code></pre> <p>Now, what if no locals shadow a global?</p> <pre><code>$GLOBAL = 20\nGLOBAL + 2  // we can omit '$' since there is no GLOBAL\n            // in this scope\n// this is just how global \"print\" function works without '$'\nprint(\"Hello, Seal!\")\n$print(\"Hello, Seal!\")\n// they are just the same\n</code></pre> <p>In Lua, everything you type without local is declared as a global. You have to type <code>local</code> to declare a local variable. But in Seal, it is kind of vice versa: unless you type <code>$</code> explicitly, anything is local.</p> <ul> <li><code>?</code> shall be combined with either field access operator (<code>.</code>) or on a variable directly.</li> </ul> <pre><code>WIDTH   //  if not defined, the program will crash\nWIDTH?  //  if not defined, return null, otherwise its value\n\nif not WIDTH?  //  one of its useful cases\n    WIDTH = 1920\n</code></pre> <p>In Lua, if you type an undefined variable name, it will silently return <code>nil</code>, unless you have attached some metatable to <code>_G</code> global table (it is dirty in my opinion). But in Seal, as you can see, you can use <code>?</code> operator to safely access to your global variable or just omit <code>?</code> to catch typos in your program.</p> <pre><code>city = {\n    name = \"Vienna\"\n}\n\ncity.population   // crashes\ncity.population?  // null\n\n// a useful case\npopulation = city.population? or 30000\n</code></pre> <p>Another Lua vs Seal situtation here. In Lua, it doesn't crash when you access to undefined field of a map. But in Seal, you can define the access behavior.</p> <ul> <li><code>-&gt;</code> is used as \"method caller\". Since Seal is a non-object-oriented language, this operator is used to imitate method-calling just like <code>:</code> operator in Lua.</li> </ul> <pre><code>person = {\n    name = \"Huseyn\",\n    talk = define(self, msg) print(self.name, \":\", msg)\n}\n\nperson-&gt;talk(\"Hello\")\n</code></pre> <p>This is also useful on calling string, list functions as methods.</p> <pre><code>\"hello\"-&gt;upper()  //  \"HELLO\"\n[]-&gt;push(0, 1, 2)\n</code></pre> <p>You can omit parentheses if you call with no arguments.</p> <pre><code>\"hello\"-&gt;upper()  //  omit ()\n\"hello\"-&gt;upper\n\"hello\"-&gt;len\n</code></pre> <ul> <li>Ternary operator in Seal is written in the following way:</li> </ul> <pre><code>a = if 5 &gt; 1 then \"hi\" else \"bye\"\n</code></pre> <ul> <li>Seal uses <code>,</code> operator from C. It is not that widely known or talked about. It can be described as a \"expression-separator\". It evaluates expressions from left to right and returns the last expression value. So it is neither a tuple like in Python, nor multiple-returned value from a function in Lua.</li> </ul> <pre><code>a = \"hi\", \"hello\"  //  returns \"hello\"\n</code></pre> <p>Don't worry. Inside list and map declarators, function calls, and lambda, unless parentheses are explicitly put, comma is used as a separator, not operator.</p> <pre><code>func(1, 2) // separator, two arguments\nfunc((1, 2)) // one argument, comma operator\nlist = [1, 2] // two elements\nlist = [(1, 2)] // one element, [2]\nlambda = define(x) x * x, 2  //  lambda = 2\nlambda = define(x) (x * x, 2)  //  lambda = a function that\n                               //  that returns only 2\n</code></pre>"},{"location":"syntax/types/","title":"Data Types","text":"<p>There are 8 types of value in Seal:</p> <ul> <li> <p>Null</p> </li> <li> <p>Boolean</p> </li> <li> <p>Integer</p> </li> <li> <p>Float</p> </li> <li> <p>String</p> </li> <li> <p>List</p> </li> <li> <p>Map</p> </li> <li> <p>Function</p> </li> </ul>"},{"location":"syntax/types/#null","title":"Null","text":"<p>This represents <code>no value</code>. <code>null</code> keyword is reserved. You can assign it to variables and other fields, but main usage of <code>null</code> data type is to represent non-existence of value. This is one of the only 'falsy' values in Seal.</p> <pre><code>a = null\nif a\n\u00a0\u00a0\u00a0\u00a0print(\"this won't be printed\")\n</code></pre>"},{"location":"syntax/types/#boolean","title":"Boolean","text":"<p>This represents truth values: <code>true</code> and <code>false</code>. These both are reserved keywords. Only <code>false</code> and <code>null</code> values are falsy values. Besides them, everything is considered as truthful values. Also, <code>not</code> unary operator which is discussed more right here creates boolean value.</p>"},{"location":"syntax/types/#integer","title":"Integer","text":"<p>Integer values can be represented in following forms:</p> <ul> <li> <p>19 (decimal)</p> </li> <li> <p>0xAF or 0xaf or 0xAf (hexadecimal)</p> </li> <li> <p>0o777 (octal)</p> </li> <li> <p>0b1010 (binary)</p> </li> </ul>"},{"location":"syntax/types/#float","title":"Float","text":"<p>Floating points can be represented in following forms:</p> <ul> <li> <p>3.14159</p> </li> <li> <p>.23 (same as 0.23)</p> </li> </ul> <p>Different operand types (such as between <code>int</code> and <code>float</code>) of binary operators are discussed here.</p>"},{"location":"syntax/types/#string","title":"String","text":"<p>Strings are immutable and garbage-collected. That means you can't modify strings like you would in C. Instead, you have to create new string object everytime you want to change it.</p> <pre><code>a = \"seal\"\nb = \"string\"-&gt;upper() // SEAL\n</code></pre> <p>Learn more about <code>-&gt;</code> right here.</p> <p>Both <code>\"</code> and <code>'</code> are the same thing. They both create string literals. Seal currently supports the following escape sequences:</p> <ul> <li> <p><code>\\n</code> (newline)</p> </li> <li> <p><code>\\t</code> (tab)</p> </li> <li> <p><code>\\\\</code> (backslash)</p> </li> <li> <p><code>\\\"</code> (double quote)</p> </li> <li> <p><code>\\'</code> (single quote)</p> </li> <li> <p><code>\\r</code> (carriage return)</p> </li> <li> <p><code>\\b</code> (backspace)</p> </li> <li> <p><code>\\f</code> (formfeed)</p> </li> <li> <p><code>\\v</code> (vertical tab)</p> </li> </ul>"},{"location":"syntax/types/#list","title":"List","text":"<p>Lists are mutable and garbage-collected. They are basically variable-sized arrays. They support negative indices which get elements from end.</p> <pre><code>a = [] // create empty list\na-&gt;push(1, 2) // same as List.push(a, 1, 2)\nprint(a[-1]) // 2\n</code></pre> <p>Lists are compared by their addresses, not values.</p> <pre><code>print([] == []) // false, since they are two different objects\n\na = [1, 2]\nb = a\nprint(a == b) // true\nprint(a != [1, 2]) // true\n</code></pre>"},{"location":"syntax/types/#map","title":"Map","text":"<p>Maps are mutable and garbage-collected. They are also know as 'Hashmaps', 'Tables', 'Dictionaries', 'Objects' or even 'Associative Arrays' from other programming languages and concepts. This is such an essential data structure for writing highly optimized codes. Seal maps can be only indexed by string keys (unlike Lua).</p> <pre><code>a = {} // create empty map\na.name = \"Seal\" // create field\na[\"features\"] = [\"Beautiful\", \"Fast\", \"Easy\"] // assign with [] syntax\n\nprint(a) // {name = 'Seal', features = ['Beautiful', 'Fast', 'Easy']}\n</code></pre> <p>Like lists, they are compared by address.</p> <pre><code>print({} == {}) // false\n\na = { key = \"value\" }\na = b\nprint(a == b) // true\nprint(a != { key == \"value\" }) // true\n</code></pre>"},{"location":"syntax/types/#function","title":"Function","text":"<p>Functions are first-class values. Meaning, they are treated as the same as other values just like lists, integers, maps etc. That means they can be passed to functions too (higher-order functions). Let's take a look at a very simple example:</p> <pre><code>define apply(f, arg)\n\u00a0\u00a0\u00a0\u00a0return f(arg)\n\nval = apply(define(x) x * x, 7)\nprint(val) // 49\n</code></pre> <p>Guess what? They are compared just like lists and maps.</p> <p>More detailed information can be found right here.</p>"},{"location":"syntax/variables/","title":"Variables","text":"<p>In Seal, there are two types of variables as you would expect: Locals and Globals.</p> <p>This language uses different syntax than other ones. Let's go through cases.</p>"},{"location":"syntax/variables/#naming","title":"Naming","text":"<p>Variable naming must follow these rules:</p> <ul> <li> <p>Starting with <code>[a-zA-Z]</code> or <code>_</code></p> </li> <li> <p>Can include <code>[a-zA-Z]</code> or <code>[0-9]</code> or <code>_</code></p> </li> </ul>"},{"location":"syntax/variables/#locals","title":"Locals","text":"<p>Any identifier you type is local.</p> <pre><code>a = 0 // local\nPI = 3.14159 // local\n</code></pre>"},{"location":"syntax/variables/#globals","title":"Globals","text":"<p>If you put <code>$</code> symbol in front of any identifier, becomes global.</p> <pre><code>i_am_local = 0 // this can be accessed only in this scope\n\n$i_am_global = 1 // this can be accessed anywhere\n\nprint(\"hello, world\") // since print is not found in locals,\n                      // it is searched in globals regardless\n\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0// same as $print(\"hello, world\")\n</code></pre>"},{"location":"syntax/variables/#variable-search-fallback","title":"Variable Search Fallback","text":"<p>If symbol name is found in current scope, it is used.</p> <pre><code>a = 0\nprint(a) // 'a' is found locally\n</code></pre> <p>If symbol is not found locally, it fallbacks to global map and searches that symbol there.</p> <pre><code>print(some_variable_name) // ERROR: 'some_variable_name is not defined'\n</code></pre> <p>If you put <code>$</code>, it does not search local names and jumps to global map immediately.</p> <pre><code>a = \"local\"\n$a = \"global\"\nprint($a) // does not print local 'a', instead searches global 'a'\n</code></pre> <p>This is how Seal prevents you from typing <code>$print</code> everytime if you want to print something unless you define a local variable called <code>print</code> in that scope. Because <code>print</code> is just a global function. God, just imagine how annoying would it be...</p>"},{"location":"syntax/variables/#preventing-error-in-lookups","title":"Preventing Error in Lookups","text":"<p>If you just want to know whether a variable is defined globally without crashing your entire program, there is a special syntax for that.</p> <p>Let me introduce you <code>?</code> operator. Instead of crashing your program this returns <code>null</code> which means <code>no value</code>.</p> <pre><code>some_var? // is it defined?\n\nname = $var? or \"John\" // if var doesn't exist or is null, use \"John\"\n</code></pre> <p>More about <code>?</code> operator can be found in here.</p>"}]}